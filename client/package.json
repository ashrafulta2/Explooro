{
  "name": "@explooro/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Explooro web client — Vite + vanilla modern CSS + modular ESM. Zero runtime dependencies.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 3001"
  },
  "//dependencies": "INTENTIONALLY EMPTY AND MUST STAY EMPTY. See the Dependency Policy in docs/prompt.md. Nothing the browser runs may come from npm — router, state store, API client, i18n, motion and charts are all hand-written in src/. Vite below is a build tool only and never ships to the browser.",
  "dependencies": {},
  "devDependencies": {
    "vite": "8.2.2"
  }
}
